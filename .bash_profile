# ------------------------------------------------------------------
# | sources bashrc so we can put all of our settings there         |
# ------------------------------------------------------------------

if [ -f ~/.bashrc ]; then
  source ~/.bashrc
fi
